# Core
Django
djangorestframework

# database
mysqlclient
django-storages
boto3
python-magic
requests

# CORS (for React frontend)
django-cors-headers

# For production (optional, useful if using gunicorn/nginx)
gunicorn

# env
python-decouple